        Ctl-Opt
                DatFmt(*ISO)
                TimFmt(*ISO)
                Debug(*Yes)
                DftActGrp(*No) ActGrp('QILE')
                Optimize(*None)
                Option(*SrcStmt:*NoDebugIO:*ShowCpy : *ExpDds)
                BndDir('PROJSVCBND');

             // AlwNull(*UsrCtl)
             // BndDir('JDBCBNDDIR')
